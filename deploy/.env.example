# Published host ports. Change these if something on your machine already owns the
# defaults.
#
#   cp deploy/.env.example deploy/.env
#
GATEWAY_PORT=8080
POSTGRES_PORT=5432
REDIS_PORT=6379
PROMETHEUS_PORT=9091
