global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: llm-gateway
    # Spring Boot exposes Prometheus metrics under the Actuator path, not /metrics.
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["gateway:8080"]
